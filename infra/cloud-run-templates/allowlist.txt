${SERVICE_NAME} ${JOB_NAME} ${PROJECT_ID} ${PROJECT_NUMBER} ${REGION} ${IMAGE} ${INGRESS} ${VPC_CONNECTOR} ${VPC_EGRESS} ${VPC_ANNOTATION_BLOCK} ${RUNTIME_SA_EMAIL} ${TIMEOUT_SECONDS} ${CONCURRENCY} ${MIN_SCALE} ${MIN_SCALE_ANNOT} ${MAX_SCALE} ${CPU} ${CPU_THROTTLING} ${MEMORY} ${ENV_BLOCK} ${VOLUME_MOUNTS_BLOCK} ${VOLUMES_BLOCK}
